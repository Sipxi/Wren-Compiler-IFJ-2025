# Стиль написания кода на C


Этот документ определяет стиль кодирования для нашего проекта.
Советую прочитать, за это снимают баллы если не додерживаться.
**Я бегло пробежался и сгенерировал c помощью ИИ**.
Если вы найдёте что-то, что не подходит под проект, то пожалуйста измените и сообщите!

---

## 🔤 Соглашения об именовании

* **Переменные**: `snake_case`

  ```c
  int buffer_size;
  char user_name[50];
  ```
* **Функции**: `snake_case`, форма глагол + существительное

  ```c
  void print_token();
  int parse_expression();
  ```
* **Константы и макросы**: `ALL_CAPS` с подчеркиванием

  ```c
  #define MAX_BUFFER 1024
  const int DEFAULT_PORT = 8080;
  ```
* **Типы (struct, enum, typedef)**: `PascalCase` или суффикс `_t`

  ```c
  typedef struct Token {
      int type;
      char *value;
  } Token;

  enum LogLevel { LOG_INFO, LOG_WARNING, LOG_ERROR };
  ```
* **Глобальные переменные**: префикс `g_`

  ```c
  int g_token_count;
  ```

---

## ВАЖНО! ВЗЯТО ИЗ ДОКУМЕНТАЦИИ
```
Úvod všech zdrojových textů musí obsahovat zakomentovaný název projektu, přihlašovací jména
a jména studujících, kteří/které se na daném souboru skutečně autorsky podíleli/y.


style:

/**
* Last edited by: Misha
* 
*/
```

## 📏 Форматирование

* **Отступы**: 4 пробела, без табов. (В VSCode должно стоять автоматически, но лучше проверить)
* **Длина строки**: максимум 100 символов.
* **Фигурные скобки**: стиль K\&R

  ```c
  if (x > 0) {
      do_something();
  } else {
      do_other();
  }
  ```
* **Пробелы**:

  * После ключевых слов: `if (x > 0)`
  * Вокруг операторов: `a + b`, не `a+b`
  * Звёздочка указателя относится к переменной, а не к типу

    ```c
    int *ptr;   // правильно
    int* ptr;   // избегать
    ```

---

## 🗂️ Файлы и модули

* **Один заголовочный файл = один исходный файл**
   - `lexer.h` ↔ `lexer.c`
   - `parser.h` ↔ `parser.c`
* **Понятные имена файлов**
   - `lexer.c` вместо `lex.c`
   - `semantic.c` вместо `sem.c`
* Имена файлов: `snake_case.c`, `snake_case.h`
* **Защита заголовков** (или можно использовать `#pragma once`):

  ```c
  #ifndef LEXER_H
  #define LEXER_H

  void tokenize(const char *source);

  #endif // LEXER_H
  ```
> Note: Он нужен для того, чтобы один и тот же заголовочный файл (.h) не был подключён несколько раз при компиляции.
  
* **Порядок include**:

  1. Собственный заголовок
  2. Системные заголовки
  3. Сторонние заголовки

  ```c
  #include "lexer.h"
  #include <stdio.h>
  #include <stdlib.h>
  ```

## ❌ Плохие практики:
- Весь код в одном файле
- Непонятные имена (`a.c`, `temp.c`)
- Смешивание логики разных модулей

---

## 📚 Функции и стиль кода

* **Одна задача на функцию** (желательно <50 строк).
* **Ранний возврат** вместо глубокой вложенности:

  ```c
  if (!user) return;
  ```
* Избегать **магических чисел** → использовать константы/макросы.
* **Обработка ошибок**:

  * Коды возврата (`0` = успех, `-1` = ошибка)
  * Использовать enum для детальных ошибок

---

## 📝 Комментарии и документация

* `//` для однострочных, `/* */` для блоков.
* Документировать каждую публичную функцию:

  ```c
  /**
   * Разбирает выражение из токенов.
   * @param tokens Список токенов.
   * @return Узел AST при успехе, NULL при ошибке.
   */
  ASTNode *parse_expression(TokenList *tokens);
  ```
* Писать комментарии о **почему**, а не очевидное **что**.


---

## 🛠️ Инструменты

* Использовать **clang-format** для единого стиля.
* Форматировать код перед пушем.
* Компиляторные предупреждения: собирать с `-Wall -Wextra -Werror`.


---
*@Sipxi Последнее обновление: 12:47 AM 9/21/2025*
